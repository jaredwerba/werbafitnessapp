# Copy to .env for local / Docker. See docs/SELF_HOSTING.md.
RP_ID=localhost
ORIGIN=http://localhost:5173
RP_NAME=openGym
WEB_PORT=8080
PORT=3000
DATA_DIR=./data
# AUTH_SECRET=  # optional; generated into data/secret if unset
# On Vercel: RP_ID/ORIGIN must match the production hostname.
# ADMIN_UIDS=
# INVITE_ONLY=
